import { createStore } from 'vuex'
import Auth from './auth';
import Chat from './chat';


export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
      Auth,
      Chat,
  }
})
