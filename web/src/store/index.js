import { createStore } from 'vuex'
import Auth from './auth';
import Todo from './todo';

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
      Auth,
      Todo
  }
})
